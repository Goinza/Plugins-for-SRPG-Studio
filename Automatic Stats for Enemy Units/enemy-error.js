//Plugin by Goinza

function throwError033(unitClass) {
    var message = "Error 33." + '\n' + "There is a problem with the class " + unitClass.getName() + "." + '\n' + "Check the custom parameter 'growths'.";
    root.msg(message);
    root.endGame();
}

function throwError034(unitClass) {
    var message = "Error 34." + '\n' + "There is a problem with the class " + unitClass.getName() + "." + '\n' + "Check the custom parameter 'growths'.";
    root.msg(message);
    root.endGame();
}
