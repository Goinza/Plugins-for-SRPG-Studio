function throwError012(skill) {
    var message = "Error 12." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'critFactor'.";
    root.msg(message);
    root.endGame();
}

function throwError013(skill) {
    var message = "Error 13." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'wand'.";
    root.msg(message);
    root.endGame();
}

function throwError014(skill) {
    var message = "Error 14." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'heal'.";
    root.msg(message);
    root.endGame();
}

function throwError015(skill) {
    var message = "Error 15." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'name'.";
    root.msg(message);
    root.endGame();
}

function throwError016(skill) {
    var message = "Error 16." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'healing'.";
    root.msg(message);
    root.endGame();
}

function throwError017(skill) {
    var message = "Error 17." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'type'.";
    root.msg(message);
    root.endGame();
}

function throwError018(skill) {
    var message = "Error 18." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'stateID'.";
    root.msg(message);
    root.endGame();
}