function throwError001(item) {
    var message = "Error 1." + '\n' + "There is a problem with the item " + item.getName() + "." + '\n' + "Check the custom parameter 'req'.";
    root.msg(message);
    root.endGame();
}

function throwError002(weapon) {
    var message = "Error 2." + '\n' + "There is a problem with the weapon " + weapon.getName() + "." + '\n' + "Check the custom parameter 'wexp'.";
    root.msg(message);
    root.endGame();
}

function throwError003(skill) {
    var message = "Error 3." + '\n' + "There is a problem with the skill " + skill.getName() + "." + '\n' + "Check the custom parameter 'multiplier'.";
    root.msg(message);
    root.endGame();
}

function throwError004(object) {
    var message = "Error 4." + '\n' + "There is a problem with the object " + object.getName() + "." + '\n' + "Check the custom parameter 'rankNew'.";
    root.msg(message);
    root.endGame();
}

function throwError005(object) {
    var message = "Error 5." + '\n' + "There is a problem with the object " + object.getName() + "." + '\n' + "Check the custom parameter 'rankPlus'.";
    root.msg(message);
    root.endGame();
}

function throwError006(object) {
    var message = "Error 6." + '\n' + "There is a problem with the object " + object.getName() + "." + '\n' + "Check the custom parameter 'rankUp'.";
    root.msg(message);
    root.endGame();
}

function throwError007(object) {
    var message = "Error 7." + '\n' + "There is a problem with the object " + object.getName() + "." + '\n' + "Check the custom parameter 'rank'.";
    root.msg(message);
    root.endGame();
}

function throwError008(unitClass) {
    var message = "Error 8." + '\n' + "There is a problem with the class " + unitClass.getName() + "." + '\n' + "Check the custom parameter 'maxRank'.";
    root.msg(message);
    root.endGame();
}

function throwError009(unitClass) {
    var message = "Error 9." + '\n' + "There is a problem with the class " + unitClass.getName() + "." + '\n' + "Check the custom parameter 'minRank'.";
    root.msg(message);
    root.endGame();
}

function throwError010(unit) {
    var message = "Error 10." + '\n' + "There is a problem with the unit " + unit.getName() + "." + '\n' + "Check the custom parameter 'rankSkills'.";
    root.msg(message);
    root.endGame();
}

function throwError011(weaponType) {
    var message = "Error 11." + '\n' + "There is a problem with the weapon type " + weaponType.getName() + "." + '\n' + "Check the custom parameter 'rankSkills'.";
    root.msg(message);
    root.endGame();
}