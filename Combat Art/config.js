//Plugin by Goinza

var TAB_COMBATART = 0; //Tab of the Original Data where combat arts are stored. Note that the first is 0, the second is 1, and so on.
var COMMAND_COMBATART = "Combat Art"; //Name of the combat arts command
var TITLE_COMBATART = "Combat Arts"; //Name of the title of the combat arts window
var ADD_COMBAT = "Learnt "; //Message when a combat arts gets added to an unit. The format is: message + [Combat Art's name]
var REMOVE_COMBAT = "Lost "; //Message when a combat arts gets removed from an unit. The format is: message + [Combat Art's name]
var ENABLE_CA_WINDOW = true; //True if the combat arts window is enabled, false if it is disabled.