//Plugin by Goinza

var CombatArtSettings = {
    TAB_COMBATART: 0, //Tab of the Original Data where combat arts are stored. Note that the first is 0, the second is 1, and so on.
    COMMAND_COMBATART: "Combat Art", //Name of the combat arts command
    TITLE_COMBATART: "Combat Arts", //Name of the title of the combat arts window
    ADD_COMBAT: "Learnt ", //Message when a combat arts gets added to an unit. The format is: message + [Combat Art's name]
    REMOVE_COMBAT: "Lost ", //Message when a combat arts gets removed from an unit. The format is: message + [Combat Art's name]
    ENABLE_CA_WINDOW: true, //True if the combat arts window is enabled, false if it is disabled.
    ENABLE_STAMINA: true, //True if you want to add the Stamina stat, false otherwise.
    STAMINA_NAME: "Sta" //Name of the Stamina stat
}